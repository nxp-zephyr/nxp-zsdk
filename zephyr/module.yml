name: zsdk
build:
  cmake: .
  kconfig: Kconfig.nxp
  #sysbuild-cmake: sysbuild
  #sysbuild-kconfig: sysbuild/Kconfig.sysbuild
  settings:
    soc_root: .
    dts_root: .
#    board_root: .
#    module_ext_root: .
